//! NeuralNote — third-party JUCE app (standalone + AU/VST3 plugin)
//! that turns monophonic audio into MIDI via the basic-pitch model.
//! Stash hands the install + launch surface to the user; the actual
//! conversion still happens inside NeuralNote itself.

pub mod commands;
