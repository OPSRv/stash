# stash-separator runtime deps. uv resolves these inside the venv at
# `Settings → Separator → Завантажити` and the resulting interpreter
# is what the in-app job spawns.
#
# `demucs.api` only exists from demucs 4.0.0 onwards — earlier
# versions don't expose the `Separator` class the sidecar imports.
# Pinning exactly to 4.0.1 (the latest 4.x as of writing) takes
# resolver ambiguity off the table; bumping has to be paired with
# a smoke test against a real song.

demucs==4.0.1
BeatNet>=1.1.0
torch>=2.2,<3
torchaudio>=2.2,<3
numpy>=1.24,<2
soundfile>=0.12
