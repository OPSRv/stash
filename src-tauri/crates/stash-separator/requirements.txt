# stash-separator runtime deps.
# Pinned ranges; CI/release-build resolves a single set of compatible
# wheels for macOS arm64. Bumping any line should be paired with a
# rebuild of the PyInstaller bundle and a smoke test of the resulting
# binary against a 30s test clip.

demucs>=4.0.1,<5
BeatNet>=1.1.0
torch>=2.2,<3
torchaudio>=2.2,<3
numpy>=1.24,<2
soundfile>=0.12
