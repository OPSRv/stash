# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.zed

# Claude Code local state (settings, scheduled tasks, worktrees scratch)
.claude/

# Storybook build output
storybook-static/

# Vitest coverage reports
coverage/

# Internal docs / scratch prototypes (not shipped)
/docs/
/stashnote/

# Local signing identity (written by scripts/sign-local.sh, sourced by
# `npm run tauri:build:signed`). Holds APPLE_SIGNING_IDENTITY so local
# builds use the user's self-signed cert without CI ever seeing it.
.env.signing
